<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ffyc.myfirstboot.dao.FeedBackDao">

    <insert id="addFeedBack">
        insert into feedback(studentID, title, info, state, time, operate_time)
        values (#{studentID}, #{title}, #{info}, 1, now(), now())
    </insert>
    <update id="updateState" parameterType="feedBack">
        update feedback
        set state=#{state},
            operator=#{operator},
            operate_time=now()
        where id = #{id}
    </update>
    <delete id="deleteFeedBack">
        delete
        from feedback
        where id = #{feedBackId}
    </delete>
    <select id="getFeedBackList" parameterType="feedBack" resultType="FeedBack">
        SELECT id,title,
        find_student_name(studentID) studentName,
        find_feedback_state(state) stateString, TIME, find_manage_account(operator) operatorAccount, operate_time
        FROM feedback
        <where>
            <if test="studentName!='' &amp;studentName!=null">
                studentID=find_student_id(#{studentName})
            </if>
            <if test="studentID!='' &amp;studentID!=null">
                and studentID=#{studentID}
            </if>
            <if test="state!='' &amp; state!=null">
                and state=#{state}
            </if>
            <if test="time!='' &amp; time!=null">
                and time like concat(#{time},'%')
            </if>
        </where>

    </select>
    <select id="getFeedBackById" resultType="FeedBack">
        select id, find_student_name(studentID) studentName, info, TIME
        from feedback
        where id=#{feedBackId}
    </select>
    <select id="getPercentOn" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM feedback
        WHERE state = 3
    </select>
    <select id="getPercentDown" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM feedback
    </select>

</mapper>